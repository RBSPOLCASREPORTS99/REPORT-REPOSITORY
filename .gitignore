# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code's local approval list. It accumulates the commands you approve,
# which can include database passwords. Never commit it — each machine keeps
# its own, and the protection must live here, not in a personal global ignore.
.claude/settings.local.json
