[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (BrowserRouter) resolve on deep links / refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
